<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.qtj.manageserver.mapper.SysUserMapper">
    <!-- 一对多结果映射 -->
    <resultMap id="UserRoleMap" type="com.qtj.manageserver.dto.SysUserDTO">
        <id column="id" property="id"/>
        <result column="username" property="username"/>
        <result column="password" property="password"/>
        <result column="nickname" property="nickname"/>
        <result column="email" property="email"/>
        <result column="phone" property="phone"/>
        <result column="status" property="status"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>

        <!-- 一对多：角色列表 -->
        <collection
            property="roleList"
            ofType="com.qtj.manageserver.dto.SysRoleDTO"
            select="selectRolesByUserId"
            column="id"
        />
    </resultMap>
    <!-- ====================== 1. 根据用户ID查询角色（子查询） ====================== -->
    <select id="selectRolesByUserId" resultType="com.qtj.manageserver.dto.SysRoleDTO">
        SELECT
            r.id,
            r.role_name,
            r.role_code,
            r.description,
            r.status
        FROM sys_user_role ur
            JOIN sys_role r
            ON ur.role_id = r.id
        WHERE ur.user_id = #{userId}
    </select>
    <!--  查询用户列表  -->
    <select id="selectUserWithRole" resultMap="UserRoleMap">
        SELECT
        u.*
        FROM sys_user u
        <where>
            <if test="query.username != null and query.username != ''">
                AND u.username LIKE CONCAT('%',#{query.username},'%')
            </if>
            <if test="query.nickname != null and query.nickname != ''">
                AND u.nickname LIKE CONCAT('%',#{query.nickname},'%')
            </if>
            <if test="query.roleId != null and query.roleId != ''">
                AND u.id IN (
                    SELECT user_id FROM sys_user_role WHERE role_id = #{query.roleId}
                )
            </if>
        </where>
    </select>
    <!--  查询用户详情  -->
    <select id="getUserDetail" resultMap="UserRoleMap">
        SELECT
            *
        FROM
            sys_user
        WHERE
            id = #{userId}
    </select>
</mapper>