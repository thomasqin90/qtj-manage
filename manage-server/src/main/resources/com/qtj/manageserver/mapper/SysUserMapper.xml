<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.qtj.manageserver.mapper.SysUserMapper">
    <!-- 一对多结果映射 -->
    <resultMap id="UserRoleMap" type="com.qtj.manageserver.dto.SysUserDTO">
        <id column="id" property="id"/>
        <result column="username" property="username"/>
        <result column="password" property="password"/>
        <result column="nickname" property="nickname"/>
        <result column="email" property="email"/>
        <result column="phone" property="phone"/>
        <result column="status" property="status"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>

        <!-- 一对多：角色列表 -->
        <collection property="roleList" ofType="com.qtj.manageserver.dto.SysRoleDTO">
            <id column="role_id" property="id"/>
            <result column="role_name" property="roleName"/>
            <result column="role_code" property="roleCode"/>
            <result column="description" property="description"/>
            <result column="role_status" property="status"/>
        </collection>
    </resultMap>
    <!--  查询用户列表  -->
    <select id="selectUserWithRole" resultMap="UserRoleMap">
        SELECT
            u.*,
            r.id          AS role_id,
            r.role_name   AS role_name,
            r.role_code   AS role_code,
            r.description AS description,
            r.status      AS role_status
        FROM sys_user u
        LEFT JOIN sys_user_role ur ON u.id = ur.user_id
        LEFT JOIN sys_role r ON ur.role_id = r.id
        ${ew.customSqlSegment}
    </select>
    <!--  查询用户详情  -->
    <select id="getUserDetail" resultMap="UserRoleMap">
        SELECT
            u.*,
            r.id          AS role_id,
            r.role_name   AS role_name,
            r.role_code   AS role_code,
            r.description AS description,
            r.status      AS role_status
        FROM
            sys_user u
                -- 关联中间表
                LEFT JOIN sys_user_role ur ON u.id = ur.user_id
                -- 关联角色表
                LEFT JOIN sys_role r ON ur.role_id = r.id
        WHERE
            u.id = #{userId}
    </select>
</mapper>